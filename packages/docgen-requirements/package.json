{
    "name": "docgen-requirements",
    "version": "0.1.0",
    "license": "MIT",
    "dependencies": {
        "gray-matter": "^3.1.1",
        "semver": "^5.5.0"
    },
    "devDependencies": {
        "@types/node": "^9.6.0",
        "@types/semver": "^5.5.0"
    },
    "scripts": {
        "build": "tsc -p ./tsconfig.json -d",
        "build:watch": "tsc -p ./tsconfig.json -d -w"
    }
}
