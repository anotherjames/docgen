{
    "name": "gatsby-plugin-page-tree",
    "version": "0.1.1",
    "license": "MIT",
    "dependencies": {
        "treeify-paths": "^1.0.1"
    },
    "scripts": {
        "build": "tsc -p ./tsconfig.json -d",
        "build:watch": "tsc -p ./tsconfig.json -d -w"
    }
}
