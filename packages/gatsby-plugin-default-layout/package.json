{
    "name": "gatsby-plugin-default-layout",
    "version": "0.1.0",
    "license": "MIT",
    "scripts": {
        "build": "tsc -p ./tsconfig.json -d",
        "build:watch": "tsc -p ./tsconfig.json -d -w"
    }
}
